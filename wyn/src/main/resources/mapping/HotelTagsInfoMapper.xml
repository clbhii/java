<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cl.wyn.core.mapper.HotelTagsInfoMapper">
    <insert id="insertBatch" parameterType="list">
        INSERT INTO hotel_tags_info ( id, hotel_id, type, code, creator_id, create_time, is_visible, is_deleted)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.hotelId},#{item.type},#{item.code},#{item.creatorId},#{item.createTime},#{item.isVisible},#{item.isDeleted})
        </foreach>

    </insert>
</mapper>
