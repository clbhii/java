<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cl.wyn.core.mapper.RoomTypeSourceInfoMapper">
    <insert id="insertBatch" parameterType="list">
        insert into room_type_source_info (id, source_id, source_hotel_id,
        source_room_type_id, hotel_id, room_type_id,
        source_type, sub_source_type, creator_id,
        create_time, modified_id, modified_time,
        is_visible, is_deleted)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id,jdbcType=VARCHAR}, #{item.sourceId,jdbcType=VARCHAR}, #{item.sourceHotelId,jdbcType=VARCHAR},
            #{item.sourceRoomTypeId,jdbcType=VARCHAR}, #{item.hotelId,jdbcType=VARCHAR}, #{item.roomTypeId,jdbcType=VARCHAR},
            #{item.sourceType,jdbcType=INTEGER}, #{item.subSourceType,jdbcType=INTEGER}, #{item.creatorId,jdbcType=VARCHAR},
            #{item.createTime,jdbcType=TIMESTAMP}, #{item.modifiedId,jdbcType=VARCHAR}, #{item.modifiedTime,jdbcType=TIMESTAMP},
            #{item.isVisible,jdbcType=INTEGER}, #{item.isDeleted,jdbcType=INTEGER})
        </foreach>

    </insert>
</mapper>
